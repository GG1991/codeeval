import sys
import math

with open(sys.argv[1], 'r') as test_cases:
    for test in test_cases:
        t = test.split( )     
        N = int(math.sqrt(len(t)))        
        for j in range(N):
          for i in range(N): 
             print t[N*(N-1)-i*N+j],     
        print
